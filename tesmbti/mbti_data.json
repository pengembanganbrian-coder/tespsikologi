export default async function handler(req, res) {
  const response = await fetch("URL_APPS_SCRIPT_KAMU", {
    method: "POST",
    body: JSON.stringify(req.body),
  });

  const result = await response.json();

  res.status(200).json(result);
}
